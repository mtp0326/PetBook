import { FinalizeMiddleware, RequestSigner } from '@aws-sdk/types';
export declare function signingMiddleware<Input extends object, Output extends object, Stream = Uint8Array>(signer: RequestSigner): FinalizeMiddleware<Input, Output, Stream>;
