import { DelayDecider, FinalizeHandler, MetadataBearer, RetryDecider } from '@aws-sdk/types';
export declare function retryMiddleware(maxRetries: number, retryDecider?: RetryDecider, delayDecider?: DelayDecider): <Output extends MetadataBearer = MetadataBearer>(next: FinalizeHandler<any, Output, any>) => FinalizeHandler<any, Output, any>;
