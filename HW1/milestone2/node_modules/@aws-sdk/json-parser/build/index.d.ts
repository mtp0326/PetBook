import { BodyParser, Decoder, Member } from "@aws-sdk/types";
export declare class JsonParser implements BodyParser {
    private readonly base64Decoder;
    constructor(base64Decoder: Decoder);
    parse<OutputType>(shape: Member, input: string): OutputType;
    private unmarshall;
}
