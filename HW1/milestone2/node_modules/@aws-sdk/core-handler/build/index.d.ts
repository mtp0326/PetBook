import { HttpHandler, MetadataBearer, ResponseParser, Terminalware } from '@aws-sdk/types';
export declare function coreHandler<OutputConstraint extends MetadataBearer, Stream = Uint8Array>(httpHandler: HttpHandler<Stream>, responseParser: ResponseParser<Stream>): Terminalware<OutputConstraint, Stream>;
