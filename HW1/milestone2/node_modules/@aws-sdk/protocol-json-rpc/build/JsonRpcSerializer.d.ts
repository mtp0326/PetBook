import { BodySerializer, HttpEndpoint, HttpRequest, OperationModel, RequestSerializer } from '@aws-sdk/types';
export declare class JsonRpcSerializer<StreamType> implements RequestSerializer<StreamType> {
    private readonly endpoint;
    private readonly bodySerializer;
    constructor(endpoint: HttpEndpoint, bodySerializer: BodySerializer);
    serialize(operation: OperationModel, input: any): HttpRequest<never>;
}
